version: 2

sources:
  - name: ai
    database: PIPEPLATTER
    schema: AI
    description: "LLM-enriched tables produced by ai/enrich_reviews.py (GROQ)."
    tables:
      - name: review_enriched
        description: "Per-review sentiment + topic + key issue from the LLM."